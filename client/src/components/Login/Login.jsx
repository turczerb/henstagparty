const Login = () => {
  return (
    <div>
      <form>
        <input placeholder="password" type="password"></input>
        <button>login</button>
      </form>
    </div>
  );
};

export default Login;
